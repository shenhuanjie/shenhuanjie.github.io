# 数据分析配置指南

**文档版本：** v2.2 Phase 3
**更新日期：** 2026-05-08
**适用对象：** 运营人员、技术人员

---

## 一、概述

本文档为 Hexo 静态博客提供数据分析配置指南。由于静态博客的技术限制，部分高级追踪功能需要手动配置或借助第三方服务实现。

### 1.1 方案选型

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| Google Analytics 4 (GA4) | 免费、功能强大、行业标准 | 需要手动添加代码 | ⭐⭐⭐⭐⭐ |
| 百度统计 | 中文界面、国内访问数据准确 | 国际化支持弱 | ⭐⭐⭐⭐ |
| Hotjar 热力图 | 直观的热力图分析 | 免费额度有限 | ⭐⭐⭐ |

### 1.2 配置架构

```
┌─────────────────────────────────────────────────────────────┐
│                     配置架构                                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  _config.yml (根配置)                                        │
│  ├── google_analytics.measurement_id  ← Hexo 插件使用       │
│  └── baidu_tongji.code              ← 百度统计               │
│                                                             │
│  themes/Chic/_config.yml (主题配置)                          │
│  └── google_analytics.tracking_id   ← Chic 主题内置 GA4     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、Google Analytics 4 配置

### 2.1 创建 GA4 属性

1. 访问 [Google Analytics](https://analytics.google.com/)
2. 点击「管理」→「创建属性」
3. 选择「GA4」属性类型
4. 填写属性名称：`shenhuanjie.github.io`
5. 设置时区和货币

### 2.2 获取 Measurement ID

1. 在 GA4 属性中，点击「数据流」
2. 添加 Web 数据流
3. 填写网站 URL：`https://shenhuanjie.github.io`
4. 获取 **Measurement ID**（格式：`G-XXXXXXXXXX`）

### 2.3 配置方式对比

#### 方式一：Chic 主题配置（推荐）

编辑 `themes/Chic/_config.yml`：

```yaml
# Google Analytics 4
google_analytics:
  enable: true
  tracking_id: G-XXXXXXXXXX  # 替换为您的 GA4 测量 ID
```

#### 方式二：根 _config.yml 配置

编辑项目根目录 `_config.yml`：

```yaml
google_analytics:
  enable: true
  measurement_id: G-XXXXXXXXXX
  options:
    anonymize_ip: true
    send_page_view: true
    debug_mode: false
```

### 2.4 手动添加 GA4 代码

如果主题不直接支持 GA4 配置，可在 `themes/chic/layout/_partial/head.ejs` 中添加：

```html
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  // 配置参数
  gtag('config', 'G-XXXXXXXXXX', {
    'anonymize_ip': true,
    'send_page_view': true
  });
</script>
```

---

## 三、事件追踪配置（v2.2 Phase 3 新增）

### 3.1 核心事件列表

| 事件名称 | 触发条件 | 事件参数 | 用途 |
|----------|----------|----------|------|
| `article_read_complete` | 阅读进度 > 90% | article_title, category | 文章完成率分析 |
| `article_read_progress` | 每 25% 进度 | article_title, progress (25/50/75) | 阅读深度分析 |
| `share_click` | 点击分享按钮 | platform (weixin/zhihu/twitter) | 分享渠道分析 |
| `search_query` | 提交搜索 | search_term | 搜索关键词分析 |
| `external_link_click` | 点击外部链接 | link_url, link_text | 跳出链接分析 |
| `toc_navigate` | 点击目录链接 | heading_id, heading_text | 目录使用分析 |

### 3.2 事件追踪代码实现

#### 阅读完成率追踪

在 `themes/Chic/layout/_partial/head.ejs` 或文章详情模板中添加：

```html
<script>
// 文章阅读完成率追踪 (v2.2 Phase 3)
(function() {
  let trackedSections = new Set();
  let scrollPercent = 0;

  function getArticleProgress() {
    const scrollHeight = document.documentElement.scrollHeight - window.innerHeight;
    return scrollHeight > 0 ? (window.scrollY / scrollHeight) * 100 : 0;
  }

  function trackEvent(name, params) {
    if (typeof gtag !== 'undefined') {
      gtag('event', name, {
        event_category: 'engagement',
        ...params
      });
    }
  }

  // 节流函数
  function throttle(func, limit) {
    let inThrottle;
    return function(...args) {
      if (!inThrottle) {
        func.apply(this, args);
        inThrottle = true;
        setTimeout(() => inThrottle = false, limit);
      }
    };
  }

  // 滚动监听
  window.addEventListener('scroll', throttle(function() {
    scrollPercent = getArticleProgress();

    // 阅读完成（滚动超过 90%）
    if (scrollPercent > 90) {
      if (!trackedSections.has('complete')) {
        trackedSections.add('complete');
        trackEvent('article_read_complete', {
          event_label: document.title,
          value: Math.round(scrollPercent)
        });
      }
    }

    // 阅读进度追踪（25%, 50%, 75%）
    [25, 50, 75].forEach(threshold => {
      if (scrollPercent >= threshold && scrollPercent < threshold + 5) {
        const key = 'progress_' + threshold;
        if (!trackedSections.has(key)) {
          trackedSections.add(key);
          trackEvent('article_read_progress', {
            event_label: document.title,
            value: threshold
          });
        }
      }
    });
  }, 1000));
})();
</script>
```

#### 分享按钮点击追踪

```html
<script>
// 分享按钮点击追踪 (v2.2 Phase 3)
(function() {
  function trackShareClick(platform) {
    if (typeof gtag !== 'undefined') {
      gtag('event', 'share_click', {
        event_category: 'social',
        event_label: platform,
        value: 1
      });
    }
  }

  // 监听分享按钮点击
  document.querySelectorAll('.share-button, .jiathis_button_weixin, .AddToShareBar a').forEach(function(button) {
    button.addEventListener('click', function(e) {
      // 尝试识别分享平台
      var platform = 'unknown';
      var classes = this.className.toLowerCase();
      var text = this.textContent.toLowerCase();

      if (classes.includes('weixin') || text.includes('微信')) {
        platform = 'weixin';
      } else if (classes.includes('zhihu') || text.includes('知乎')) {
        platform = 'zhihu';
      } else if (classes.includes('twitter') || text.includes('twitter')) {
        platform = 'twitter';
      } else if (classes.includes('weibo') || text.includes('微博')) {
        platform = 'weibo';
      }

      trackShareClick(platform);
    });
  });
})();
</script>
```

#### 外部链接点击追踪

```html
<script>
// 外部链接点击追踪 (v2.2 Phase 3)
(function() {
  function isExternalLink(url) {
    return url && (url.startsWith('http://') || url.startsWith('https://'))
      && !url.includes(window.location.hostname);
  }

  document.querySelectorAll('a[href]').forEach(function(link) {
    link.addEventListener('click', function(e) {
      var url = this.getAttribute('href');
      if (isExternalLink(url)) {
        if (typeof gtag !== 'undefined') {
          gtag('event', 'external_link_click', {
            event_category: 'navigation',
            event_label: url.substring(0, 100), // 截断过长URL
            link_text: this.textContent.substring(0, 50)
          });
        }
      }
    });
  });
})();
</script>
```

### 3.3 GA4 自定义事件验证

1. 安装 [GA4 DebugView](https://chrome.google.com/webstore/detail/google-analytics-debugger/jnkmfdileelhofjcijadephnlnkepbka) 扩展
2. 开启调试模式访问网站
3. 触发事件后在 DebugView 中查看实时事件

---

## 四、百度统计配置（可选）

### 4.1 获取百度统计代码

1. 注册 [百度统计](https://tongji.baidu.com/)
2. 添加网站并验证所有权
3. 获取统计代码（格式：`hm.src = 'https://hm.baidu.com/hm.js?xxxxxxxx'`）

### 4.2 配置方式

编辑项目根目录 `_config.yml`：

```yaml
baidu_tongji:
  enable: true
  code: 'xxxxxxxx'  # 替换为您的百度统计 ID
```

### 4.3 手动添加代码

在 `themes/Chic/layout/_partial/head.ejs` 中添加：

```html
<!-- 百度统计 -->
<script>
var _hmt = _hmt || [];
(function() {
  var hm = document.createElement("script");
  hm.src = "https://hm.baidu.com/hm.js?xxxxxxxx"; // 替换为您的代码
  var s = document.getElementsByTagName("script")[0];
  s.parentNode.insertBefore(hm, s);
})();
</script>
```

---

## 五、热力图集成（可选）

### 5.1 Hotjar 配置

1. 注册 [Hotjar](https://www.hotjar.com/)
2. 创建站点，获取 Hotjar Tracking Code
3. 添加到主题 `head` 区域

---

## 六、转化漏斗分析

### 6.1 定义转化漏斗

建议的漏斗模型：

```
首页访问 → 文章列表 → 文章详情 → 阅读完成 → 分享/互动
```

### 6.2 GA4 漏斗配置

在 GA4 中通过「配置」→「漏斗」创建：

1. **步骤 1：首页访问** - 选择「页面浏览」事件，路径包含 `/`
2. **步骤 2：文章列表** - 路径包含 `/archives` 或 `/categories`
3. **步骤 3：文章详情** - 路径匹配 `/:year/:month/:day/:hash/`
4. **步骤 4：阅读完成** - 触发 `article_read_complete` 事件
5. **步骤 5：分享/互动** - 触发 `share_click` 事件

---

## 七、流量来源追踪

### 7.1 UTM 参数规范

| 参数 | 说明 | 示例 |
|------|------|------|
| `utm_source` | 来源平台 | `utm_source=weixin` |
| `utm_medium` | 营销渠道 | `utm_medium=social` |
| `utm_campaign` | 营销活动 | `utm_campaign=ai_weekly` |

### 7.2 常用 UTM 模板

```
# 微信公众号
https://shenhuanjie.github.io/?utm_source=weixin&utm_medium=article&utm_campaign=ai_weekly

# 知乎专栏
https://shenhuanjie.github.io/?utm_source=zhihu&utm_medium=article&utm_campaign=ai_weekly

# 微信朋友圈
https://shenhuanjie.github.io/?utm_source=weixin_moments&utm_medium=social&utm_campaign=ai_weekly
```

---

## 八、配置检查清单

- [ ] GA4 Measurement ID 已配置（根 _config.yml 或主题 _config.yml）
- [ ] 代码已添加到主题文件
- [ ] 基础页面浏览追踪正常
- [ ] 阅读完成事件追踪已部署
- [ ] 分享点击事件追踪已部署
- [ ] 外部链接点击追踪已部署
- [ ] UTM 参数规范已制定
- [ ] 漏斗分析模型已配置

---

## 九、常见问题

### Q1: GA4 代码添加后不生效？

1. 确认 Measurement ID 正确
2. 清除浏览器缓存
3. 使用 GA4 Debug 模式验证
4. 检查浏览器控制台是否有错误
5. 确认是 `measurement_id`（根配置）还是 `tracking_id`（主题配置）

### Q2: 如何验证追踪是否正常？

1. 安装 [GA4 DebugView](https://chrome.google.com/webstore/detail/google-analytics-debugger/jnkmfdileelhofjcijadephnlnkepbka) 扩展
2. 访问网站并触发事件
3. 在 DebugView 中查看实时事件
4. 等待 24-48 小时在标准报告中查看数据

### Q3: 静态博客能追踪哪些指标？

| 指标 | 支持情况 | 说明 |
|------|----------|------|
| 页面浏览量 (PV) | ✅ 完全支持 | 自动追踪 |
| 独立访客数 (UV) | ✅ 完全支持 | 通过 Cookie 识别 |
| 用户停留时长 | ✅ 通过事件估算 | 基于页面停留时间计算 |
| 跳出率 | ✅ 完全支持 | 自动计算 |
| 滚动完成率 | ⚠️ 需手动配置 | 需添加事件追踪代码 |
| 分享点击 | ⚠️ 需手动配置 | 需添加事件追踪代码 |
| 外部链接点击 | ⚠️ 需手动配置 | 需添加事件追踪代码 |

### Q4: 根配置和主题配置有什么区别？

- **根 _config.yml**：供 Hexo 插件或自定义集成使用
- **主题 _config.yml**：Chic 主题内置的 GA4 支持，直接在主题中处理追踪

建议使用主题配置（更简单），或根据需要选择其一。

---

## 十、后续步骤

完成配置后，建议：

1. 等待 24-48 小时数据积累
2. 在 GA4 中创建自定义报告
3. 设置周报自动发送（参见「周报自动化指南.md」）
4. 定期检查数据异常情况
5. 根据数据优化内容策略

---

## 附录：事件追踪代码片段汇总

所有事件追踪代码可添加到 `themes/Chic/layout/_partial/head.ejs` 或文章详情模板中。建议将代码包裹在匿名函数中以避免全局变量污染。

**代码加载顺序：**
1. GA4 基础脚本（主题已处理）
2. 阅读完成率追踪
3. 分享点击追踪
4. 外部链接点击追踪

---

## 附录 B：用户行为分析方法论（v2.7 Phase 15 新增）

### B.1 阅读深度分析

#### 核心指标定义

| 指标名称 | 定义 | 计算方式 | 目标值 |
|----------|------|----------|--------|
| 阅读完成率 | 用户滚动到文章底部的比例 | scroll_to_bottom / article_pv | > 30% |
| 阅读进度分布 | 用户阅读进度的区间分布 | 25%/50%/75%/90% 节点统计 | 75% > 50% > 90% |
| 平均阅读时长 | 用户在文章页的平均停留时间 | engagement_time / sessions | > 2 分钟 |
| 阅读速度 | 用户平均每分钟阅读的字数 | words / reading_time | 400-800 字/分钟 |

#### 阅读深度数据获取

```javascript
// 阅读深度追踪事件
gtag('event', 'article_read_progress', {
  article_title: document.title,
  scroll_percent: Math.round(scrollPercent),
  reading_time: readingTime
});
```

#### 阅读深度分析维度

| 分析维度 | 指标组合 | 分析目的 |
|----------|----------|----------|
| 内容吸引力 | 完成率 + 阅读时长 | 识别高质量内容 |
| 内容长度匹配度 | 阅读时长 / 字数 | 判断内容是否过长或过短 |
| 用户参与度 | 进度节点分布 | 优化内容结构 |
| 跳出时机 | 70% 跳出率 | 识别内容结束点问题 |

### B.2 内容偏好分析

#### 核心指标定义

| 指标名称 | 定义 | 计算方式 | 用途 |
|----------|------|----------|------|
| 分类热度 | 各分类的 PV/UV 占比 | category_pv / total_pv | 内容方向决策 |
| 标签覆盖率 | 文章标签的覆盖度 | tagged_articles / total_articles | 标签体系优化 |
| 热门内容类型 | 不同类型文章的绩效对比 | PV_comparison_by_type | 内容类型决策 |
| 更新频率响应 | 发布频率与流量变化关系 | traffic_delta / publish_delta | 发布时机优化 |

#### 内容偏好数据获取

```javascript
// 内容偏好追踪事件
gtag('event', 'content_engagement', {
  article_category: articleCategory,
  article_tags: articleTags.join(','),
  article_type: contentType,
  engagement_score: engagementTime * completionRate
});
```

#### 内容偏好分析维度

| 分析维度 | 指标组合 | 分析目的 |
|----------|----------|----------|
| 分类偏好 | 分类 PV + 完成率 + 分享率 | 确定主力内容方向 |
| 长度偏好 | 文章字数 + 阅读完成率 | 优化文章长度 |
| 类型偏好 | 技术教程 vs 产业观察 vs 资讯 | 内容类型分配 |
| 时效性偏好 | 新文章 vs 老文章流量 | 内容更新频率 |

### B.3 来源分析

#### 核心指标定义

| 指标名称 | 定义 | 计算方式 | 用途 |
|----------|------|----------|------|
| 流量来源分布 | 各渠道带来的会话占比 | source_sessions / total_sessions | 渠道优先级 |
| 来源质量对比 | 不同来源的跳出率/完成率对比 | source_bounce_rate, source_completion | 渠道质量评估 |
| 入口页面分析 | 用户首次访问的页面分布 | landing_page / total_entries | 落地页优化 |
| UTM 参数追踪 | 营销活动的追踪效果 | utm_campaign performance | 活动效果评估 |

#### 来源分析维度

| 分析维度 | 指标组合 | 分析目的 |
|----------|----------|----------|
| 渠道效果 | 来源会话 + 转化率 + 跳出率 | 确定主力渠道 |
| 活动效果 | UTM 会话 + 目标转化 | 评估营销ROI |
| 外部链接来源 | 外链 Referrer 分布 | 发现外部推广机会 |
| 社交媒体来源 | 微信/知乎/微博流量对比 | 社交平台优先级 |

---

## 附录 C：转化漏斗分析（v2.7 Phase 15 新增）

### C.1 漏斗模型定义

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          用户转化漏斗模型 (v2.7 Phase 15)                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐│
│   │  访问   │ →  │  浏览   │ →  │  阅读   │ →  │  互动   │ →  │  转化   ││
│   │  入口   │    │  文章   │    │  完成   │    │  行为   │    │  目标   ││
│   └─────────┘    └─────────┘    └─────────┘    └─────────┘    └─────────┘│
│       ↓             ↓             ↓             ↓             ↓            │
│    sessions    article_pv    read_complete   engagement   conversion      │
│       ↓             ↓             ↓             ↓             ↓            │
│     100%      60-80%        30-50%         10-20%         1-5%             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### C.2 漏斗阶段指标定义

| 漏斗阶段 | 触发条件 | GA4 事件 | 计算公式 |
|----------|----------|----------|----------|
| 1. 访问入口 | 用户进入站点 | `session_start` | 基准值 100% |
| 2. 浏览文章 | 访问文章详情页 | `page_view` (article) | 会话中访问文章的比例 |
| 3. 阅读完成 | 滚动超过 90% | `article_read_complete` | 完成阅读 / 文章浏览 |
| 4. 互动行为 | 分享/收藏/评论 | `share_click`, `favorite_article` | 互动用户 / 阅读完成用户 |
| 5. 转化目标 | 关注/订阅等 | `subscribe`, `conversion` | 转化用户 / 总用户 |

### C.3 漏斗分析关键指标

| 指标名称 | 定义 | 计算方式 | 理想范围 |
|----------|------|----------|----------|
| 漏斗转化率 | 每步的留存比例 | next_step / current_step | 逐步递减 |
| 总转化率 | 从入口到转化的整体转化 | final_conversion / total_entry | 0.5-2% |
| 漏斗丢失率 | 每步流失的用户比例 | 1 - (next_step / current_step) | < 50%/步 |
| 漏斗优化空间 | 当前与目标的差距 | target - actual | 越大越需要优化 |

### C.4 漏斗数据获取

```javascript
// 漏斗各阶段事件追踪
// 1. 访问入口 - GA4 自动追踪 session_start

// 2. 浏览文章 - GA4 自动追踪 page_view
// 通过过滤 page_location 包含文章路径

// 3. 阅读完成
gtag('event', 'article_read_complete', {
  article_title: document.title,
  scroll_percent: 90
});

// 4. 互动行为
gtag('event', 'share_click', { platform: 'wechat' });
gtag('event', 'favorite_article', { action: 'add' });

// 5. 转化目标
gtag('event', 'conversion', {
  event_category: 'subscribe',
  method: 'wechat_guide'
});
```

### C.5 漏斗优化策略

| 流失环节 | 诊断方法 | 优化策略 |
|----------|----------|----------|
| 访问→浏览 (流失 > 40%) | 检查首页跳出率 | 优化首屏内容，增加文章预览 |
| 浏览→阅读完成 (流失 > 50%) | 检查文章开头跳出 | 优化开头段落，增加悬念 |
| 阅读完成→互动 (流失 > 70%) | 检查互动入口位置 | 优化分享按钮位置，增加评论引导 |
| 互动→转化 (流失 > 80%) | 检查转化路径 | 简化转化流程，增加紧迫感 |