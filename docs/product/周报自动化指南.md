# 周报自动化指南

**文档版本：** v2.1 Phase 3
**更新日期：** 2026-05-08
**适用对象：** 运营人员、内容编辑

---

## 一、概述

静态博客项目无法像动态网站那样直接连接数据库，因此周报自动化需要借助外部工具实现。本文档提供半自动化和全自动化两种方案。

---

## 二、周报内容结构

### 2.1 周报模板

```markdown
# {博客名称} 周报 - {年份}年第{周数}周

**报告周期：** {开始日期} ~ {结束日期}
**生成日期：** {生成日期}

---

## 一、核心数据概览

| 指标 | 本周 | 上周 | 环比变化 |
|------|------|------|----------|
| 访问量 (PV) | - | - | - |
| 独立访客 (UV) | - | - | - |
| 平均停留时长 | - | - | - |
| 跳出率 | - | - | - |

---

## 二、内容发布统计

| 类型 | 本周发布 | 累计 |
|------|----------|------|
| AI 资讯 | - | - |
| 产业观察 | - | - |
| 技术解读 | - | - |
| **合计** | - | - |

---

## 三、热门文章 TOP 5

| 排名 | 文章标题 | 阅读量 | 分享数 |
|------|----------|--------|--------|
| 1 | - | - | - |
| 2 | - | - | - |
| 3 | - | - | - |
| 4 | - | - | - |
| 5 | - | - | - |

---

## 四、流量来源分析

| 来源 | 占比 | 变化趋势 |
|------|------|----------|
| 直接访问 | - | - |
| 搜索引擎 | - | - |
| 社交媒体 | - | - |
| 外链引用 | - | - |

---

## 五、本周大事记

- [ ]

---

## 六、下周计划

- [ ]

---

## 七、问题与改进

### 问题
-

### 改进措施
-

---

**运营人员：** {姓名}
**联系方式：** {邮箱}
```

---

## 三、半自动化方案（推荐）

### 3.1 数据提取流程

```
┌─────────────────────────────────────────────────────────────┐
│                     周报生成流程                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 手动提取数据（10分钟）                                   │
│     ├── GA4 后台导出数据                                    │
│     ├── 整理 Excel 表格                                    │
│     └── 填写周报模板                                        │
│                         ↓                                   │
│  2. 数据分析（5分钟）                                       │
│     ├── 对比上周数据                                        │
│     ├── 标记异常指标                                        │
│     └── 提炼关键洞察                                        │
│                         ↓                                   │
│  3. 内容填充（5分钟）                                       │
│     ├── 填写本周大事记                                      │
│     ├── 制定下周计划                                        │
│     └── 补充问题与改进                                      │
│                         ↓                                   │
│  4. 发布存档（2分钟）                                       │
│     ├── 导出 PDF 格式                                      │
│     └── 保存到周报归档目录                                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 GA4 数据提取步骤

#### Step 1：访问量数据

1. 登录 GA4 → 报告 → 概览
2. 设置日期范围：本周
3. 记录关键指标：用户数、页面浏览量、会话数、平均参与时长

#### Step 2：用户获取数据

1. 报告 → 用户获取 → 流量获取
2. 导出「会话来源」数据
3. 整理各来源占比

#### Step 3：内容表现

1. 报告 → 参与度 → 页面和屏幕
2. 过滤「页面路径包含发布日期」
3. 按页面浏览量排序，导出 TOP 10

#### Step 4：事件数据

1. 报告 → 参与度 → 事件
2. 检查自定义事件触发次数
3. 记录关键事件：`article_read_complete`, `share_click`

### 3.3 数据整理模板

创建 `docs/运营/周报数据-YYYY-MM-DD.xlsx` 表格：

```
工作表 1: 核心指标
┌────────────┬────────┬────────┐
│ 指标       │ 本周   │ 上周   │
├────────────┼────────┼────────┤
│ PV         │        │        │
│ UV         │        │        │
│ 平均时长   │        │        │
│ 跳出率     │        │        │
└────────────┴────────┴────────┘

工作表 2: TOP 10 文章
┌────┬──────┬─────┬─────┐
│ 排名 │ 标题 │ PV  │ 分享 │
├────┼──────┼─────┼─────┤
│    │      │     │     │
└────┴──────┴─────┴─────┘

工作表 3: 流量来源
┌────────┬───────┐
│ 来源   │ 占比  │
├────────┼───────┤
│        │       │
└────────┴───────┘
```

---

## 四、自动化方案（高级）

### 4.1 Google Apps Script 方案

对于技术能力较强的团队，可使用 Google Apps Script 自动抓取 GA4 数据：

```javascript
// GA4 Data API 调用示例
// 注意：需要启用 GA4 Data API 并配置 OAuth2

function getGA4Report() {
  const propertyId = 'XXXXXXXXXX'; // GA4 Property ID

  const response = UrlFetchApp.fetch(
    `https://analyticsdata.googleapis.com/v1beta/properties/${propertyId}:runReport`,
    {
      method: 'POST',
      headers: {
        Authorization: 'Bearer ' + getService().getAccessToken(),
        'Content-Type': 'application/json'
      },
      payload: JSON.stringify({
        dateRanges: [{
          startDate: '7daysAgo',
          endDate: 'today'
        }],
        metrics: [
          { name: 'screenPageViews' },
          { name: 'totalUsers' },
          { name: 'averageSessionDuration' },
          { name: 'bounceRate' }
        ],
        dimensions: [{ name: 'date' }]
      })
    }
  );

  return JSON.parse(response.getContentText());
}
```

### 4.2 GitHub Actions 自动化

创建 `.github/workflows/weekly-report.yml`：

```yaml
name: Weekly Report Generation

on:
  schedule:
    # 每周一 09:00 UTC 运行 (= 17:00 北京时间)
    - cron: '0 9 * * 1'
  workflow_dispatch: # 手动触发

jobs:
  generate-report:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Fetch GA4 data
        run: node scripts/fetch-ga4-data.js
        env:
          GA4_PROPERTY_ID: ${{ secrets.GA4_PROPERTY_ID }}
          GA4_CREDENTIALS: ${{ secrets.GA4_CREDENTIALS }}

      - name: Generate weekly report
        run: node scripts/generate-weekly-report.js

      - name: Commit report
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add docs/运营/周报/
          git commit -m "chore: 生成 $(date +%Y-%m-%d) 周报" || echo "No changes to commit"

      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
```

### 4.3 数据抓取脚本示例

创建 `scripts/fetch-ga4-data.js`：

```javascript
/**
 * GA4 数据抓取脚本
 * 用法: node scripts/fetch-ga4-data.js
 *
 * 前置条件:
 * 1. 设置 GA4_PROPERTY_ID 环境变量
 * 2. 设置 GA4_CREDENTIALS 环境变量（OAuth2 Token 或 Service Account Key）
 */

const https = require('https');
const fs = require('fs');
const path = require('path');

// 获取环境变量
const propertyId = process.env.GA4_PROPERTY_ID;
const credentials = process.env.GA4_CREDENTIALS;

if (!propertyId || !credentials) {
  console.error('请设置 GA4_PROPERTY_ID 和 GA4_CREDENTIALS 环境变量');
  process.exit(1);
}

// API 配置
const baseUrl = 'analyticsdata.googleapis.com';
const endpoint = `/v1beta/properties/${propertyId}:runReport`;

// 获取本周数据
async function fetchWeeklyData() {
  const dates = getWeekDateRange();

  const payload = {
    dateRanges: [{
      startDate: dates.startDate,
      endDate: dates.endDate
    }],
    metrics: [
      { name: 'screenPageViews' },
      { name: 'totalUsers' },
      { name: 'averageSessionDuration' },
      { name: 'bounceRate' }
    ],
    dimensions: [{ name: 'date' }]
  };

  // 发送 API 请求
  const data = await makeRequest(endpoint, payload);

  // 处理并保存数据
  const outputPath = path.join(__dirname, '../docs/运营/周报', `ga4-data-${dates.endDate}.json`);
  fs.writeFileSync(outputPath, JSON.stringify(data, null, 2));

  console.log(`数据已保存到: ${outputPath}`);
  return data;
}

// 辅助函数
function getWeekDateRange() {
  const now = new Date();
  const dayOfWeek = now.getDay();
  const startDate = new Date(now);
  startDate.setDate(now.getDate() - dayOfWeek - 6);
  const endDate = new Date(now);
  endDate.setDate(now.getDate() - dayOfWeek);

  return {
    startDate: formatDate(startDate),
    endDate: formatDate(endDate)
  };
}

function formatDate(date) {
  return date.toISOString().split('T')[0];
}

function makeRequest(endpoint, payload) {
  return new Promise((resolve, reject) => {
    const data = JSON.stringify(payload);

    const options = {
      hostname: baseUrl,
      path: endpoint,
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Content-Length': data.length,
        Authorization: `Bearer ${credentials}`
      }
    };

    const req = https.request(options, res => {
      let body = '';
      res.on('data', chunk => body += chunk);
      res.on('end', () => {
        if (res.statusCode >= 200 && res.statusCode < 300) {
          resolve(JSON.parse(body));
        } else {
          reject(new Error(`API 请求失败: ${res.statusCode} ${body}`));
        }
      });
    });

    req.on('error', reject);
    req.write(data);
    req.end();
  });
}

// 执行
fetchWeeklyData().catch(console.error);
```

---

## 五、周报发布流程

### 5.1 发布检查清单

- [ ] 数据已从 GA4 提取
- [ ] 核心指标已填写
- [ ] 热门文章已更新
- [ ] 流量来源已分析
- [ ] 本周大事记已填写
- [ ] 下周计划已制定
- [ ] 格式检查通过
- [ ] PDF 已导出

### 5.2 周报归档

建议的归档结构：

```
docs/
└── 运营/
    └── 周报/
        ├── 2026/
        │   ├── 周报-2026-W01.md
        │   ├── 周报-2026-W01.pdf
        │   ├── 周报-2026-W02.md
        │   └── ...
        └── 周报数据/
            ├── ga4-data-2026-01-05.json
            └── ga4-data-2026-01-12.json
```

---

## 六、效率优化建议

### 6.1 减少手动操作

| 优化项 | 预期节省时间 |
|--------|-------------|
| 使用 GA4 快捷键 | ~2 分钟 |
| 预设 Excel 模板 | ~3 分钟 |
| 使用周报模板 | ~5 分钟 |
| **合计** | **~10 分钟/周** |

### 6.2 关键指标速查

在 GA4 中创建「快捷报告」：

1. 报告 → 库 → 新建快捷报告
2. 添加核心指标
3. 保存为「周报速查」

---

## 七、常见问题

### Q1: GA4 数据导出格式？

推荐使用「CSV」或「Google 表格」格式，便于后续数据处理。

### Q2: 如何获取上周对比数据？

在 GA4 中设置「比较」时间段，选择「上周同期」即可。

### Q3: 突发事件如何记录？

在周报「本周大事记」中单独标记「异常事件」，包含时间、现象、可能原因。

### Q4: 自动化脚本配置复杂吗？

GA4 Data API 需要：
1. Google Cloud 项目启用 API
2. OAuth2 或 Service Account 认证
3. 脚本开发调试

相对复杂，建议技术团队协助完成。

---

## 八、后续步骤

1. 复制周报模板到 `docs/运营/周报/` 目录
2. 创建数据提取的 Excel 模板
3. （可选）配置 GitHub Actions 自动化
4. 练习手动提取数据流程，熟悉 GA4 界面
5. 优化模板，形成团队规范

---

## 九、参考资源

- [GA4 官方文档](https://support.google.com/analytics/)
- [GA4 Data API](https://developers.google.com/analytics/data/v1/)
- [Google Apps Script 文档](https://developers.google.com/apps-script)
- [GitHub Actions 文档](https://docs.github.com/actions)
